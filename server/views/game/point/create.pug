extends ../../base/page

block content
    unless created
        p.
            Vul de naam en locatie in voor het punt dat u wilt aanmaken.#[br]
            #[br]
            Gebruik de longitude en latitude om de gewenste locatie aan te duiden.
            U kunt een punt op de kaart plaatsen om automatisch de longitude en latitude te bepalen.
        br

        form(method='POST', action=page.url + '/#')
            label(for='field-point-name') Puntnaam:
            input(type='text', name='field-point-name', id='field-point-name', value='', data-clear-btn='true', data-randomize='true')

            #point-map-container

            label(for='field-point-lat') Latitude:
            input(type='text', name='field-point-lat', id='field-point-lat', value='', data-clear-btn='true', data-randomize='true')
            label(for='field-point-lng') Longitude:
            input(type='text', name='field-point-lng', id='field-point-lng', value='', data-clear-btn='true', data-randomize='true')

            br
            .button-list
                button.ui-btn.ui-btn-raised.clr-primary(type='submit', data-ajax='false')
                    i.zmdi.zmdi-pin-drop.ui-pull-left
                    | &nbsp;&nbsp;Punt aanmaken
                a.ui-btn(href='/', data-rel='back', data-direction='reverse', title='Ga terug')
                    i.zmdi.zmdi-arrow-back.ui-pull-left
                    | &nbsp;&nbsp;Ga terug

    else
        p Het punt is succesvol aangemaakt.

        div(align="center")
            table.table-list.ui-responsive
                tr: td Punt
                    td= point.name

        p Klik op de onderstaande knop om terug te gaan naar het punten overzicht.

        br
        .button-list
            a.ui-btn.ui-btn-raised.clr-primary(href='/game/' + game.id + '/point/create', title='Nieuw punt aanmaken', data-ajax='false')
                i.zmdi.zmdi-pin-drop.ui-pull-left
                | &nbsp;&nbsp;Nieuw punt aanmaken
            a.ui-btn(href='/game/' + game.id + '/points', title='Puntenoverzicht', data-ajax='false')
                i.zmdi.zmdi-pin.ui-pull-left
                | &nbsp;&nbsp;Puntenoverzicht
            a.ui-btn(href='/game/' + game.id, title='Speloverzicht', data-ajax='false')
                i.zmdi.zmdi-comment-edit.ui-pull-left
                | &nbsp;&nbsp;Speloverzicht
            a.ui-btn(href='/game/' + game.id + '/point/' + point.id, title='Punt bekijken', data-ajax='false')
                i.zmdi.zmdi-zoom-in.ui-pull-left
                | &nbsp;&nbsp;Punt bekijken
